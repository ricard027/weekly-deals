import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Navigation from "@/components/Navigation";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TechDeals - As Melhores Ofertas em Tecnologia",
  description:
    "Descubra produtos de tecnologia incríveis a preços imbatíveis. Nossas ofertas especiais trazem as melhores promoções em smartphones, notebooks, gadgets e muito mais.",
  keywords:
    "tecnologia, ofertas, promoções, smartphones, notebooks, gadgets, tech deals",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
        suppressHydrationWarning={true}
      >
        <Navigation />
        {children}
      </body>
    </html>
  );
}
